extends ../layouts/default

block header
  include ../mixins/mixins

  +headerLg()


block content

  include ../mixins/mixins
  - var data1 = portfolioData.atelier.slice(0, Math.floor(portfolioData.atelier.length / 2))
  - var data2 = portfolioData.atelier.slice(Math.floor(portfolioData.atelier.length / 2))
  +gallery(data1, data2)
  +mobileGallery(portfolioData.atelier)