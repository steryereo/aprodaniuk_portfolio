extends ../../layouts/default

block header
  include ../../mixins/mixins

  +headerSm()


block content

  include ../../mixins/mixins
  -
    var item;
    for (var i = 0; i < portfolioData.design.length; i++) {
      if (portfolioData.design[i].title === "Chronicle Wine Competition") {
        item = portfolioData.design[i];
      }
    }
  +project(item)